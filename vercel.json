{
    "version": 2,
    "buildCommand": "npm run build",
    "builds": [
        {
            "src": "./dist/api.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}
