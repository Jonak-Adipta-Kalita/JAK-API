{
    "version": 3,
    "builds": [
        {
            "src": "./dist/api.js",
            "use": "@vercel/node",
            "config": {
                "buildCommand": "npm run build"
            }
        }
    ],
    "routes": [
        {
            "handle": "filesystem",
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}
